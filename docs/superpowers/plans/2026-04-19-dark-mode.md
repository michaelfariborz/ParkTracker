# Dark Mode Toggle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a dark/light mode toggle to the navbar that persists via `localStorage` and defaults to the OS `prefers-color-scheme` setting, covering all pages.

**Architecture:** Bootstrap 5.3.3's native `data-bs-theme` attribute drives theming. A tiny inline `<script>` in `<head>` applies the theme before the first paint (no flash). A `ThemeToggle.razor` component in the navbar calls two JS helpers (`getTheme`/`setTheme`) via `IJSRuntime` to read and write the theme.

**Tech Stack:** Blazor Server (.NET 10), Bootstrap 5.3.3, vanilla JS (`IJSRuntime` interop), `localStorage`

---

## File Map

| Action | Path | Responsibility |
|---|---|---|
| Modify | `ParkTracker/Components/App.razor` | Add inline init script to `<head>`; add `<script src="js/theme.js">` to body |
| Create | `ParkTracker/wwwroot/js/theme.js` | `getTheme()` and `setTheme()` JS helpers |
| Create | `ParkTracker/Components/Layout/ThemeToggle.razor` | Sun/moon toggle button, JS interop |
| Modify | `ParkTracker/Components/Layout/NavMenu.razor` | Add `<ThemeToggle />` to right nav |
| Modify | `ParkTracker/wwwroot/app.css` | `[data-bs-theme="dark"]` overrides for custom green brand colors |

---

### Task 1: Create JS theme helpers

**Files:**
- Create: `ParkTracker/wwwroot/js/theme.js`

- [ ] **Step 1: Create the file**

```js
window.getTheme = () => document.documentElement.getAttribute('data-bs-theme') ?? 'light';

window.setTheme = (theme) => {
    document.documentElement.setAttribute('data-bs-theme', theme);
    localStorage.setItem('theme', theme);
};
```

- [ ] **Step 2: Verify the file exists**

```bash
cat ParkTracker/wwwroot/js/theme.js
```
Expected: the two function definitions printed to stdout.

- [ ] **Step 3: Commit**

```bash
git add ParkTracker/wwwroot/js/theme.js
git commit -m "feat: add JS theme helpers for dark mode"
```

---

### Task 2: Add theme init script and load theme.js in App.razor

**Files:**
- Modify: `ParkTracker/Components/App.razor`

The init script must go **before** the Bootstrap stylesheet link so the `data-bs-theme` attribute is present before Bootstrap CSS is parsed. This prevents a flash of the wrong theme.

Current `App.razor` `<head>` order:
```html
<meta charset="utf-8" />
<meta name="viewport" ... />
<base href="/" />
<ResourcePreloader />
<link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
```

- [ ] **Step 1: Add the inline init script before the Bootstrap stylesheet**

In `ParkTracker/Components/App.razor`, insert the `<script>` block immediately after `<ResourcePreloader />` and before the first `<link rel="stylesheet">`:

```html
<ResourcePreloader />
<script>
    (function() {
        var stored = localStorage.getItem('theme');
        var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        var theme = stored !== null ? stored : (prefersDark ? 'dark' : 'light');
        document.documentElement.setAttribute('data-bs-theme', theme);
    })();
</script>
<link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
```

- [ ] **Step 2: Add the theme.js script tag in the body**

In the same file, in the `<body>` section, add `theme.js` after the Bootstrap bundle script and before `blazor.web.js`:

```html
<script src="@Assets["lib/bootstrap/dist/js/bootstrap.bundle.min.js"]"></script>
<script src="js/theme.js"></script>
<script src="@Assets["_framework/blazor.web.js"]"></script>
```

- [ ] **Step 3: Build to verify no compile errors**

```bash
dotnet build ParkTracker
```
Expected: `Build succeeded. 0 Error(s)`

- [ ] **Step 4: Commit**

```bash
git add ParkTracker/Components/App.razor
git commit -m "feat: add theme init script and load theme.js"
```

---

### Task 3: Create ThemeToggle component

**Files:**
- Create: `ParkTracker/Components/Layout/ThemeToggle.razor`

- [ ] **Step 1: Create the component**

```razor
@inject IJSRuntime JS
@rendermode InteractiveServer

<button class="nav-link btn btn-link theme-toggle-btn"
        aria-label="@(_isDark ? "Switch to light mode" : "Switch to dark mode")"
        title="@(_isDark ? "Switch to light mode" : "Switch to dark mode")"
        @onclick="ToggleTheme">
    @(_isDark ? "☀️" : "🌙")
</button>

@code {
    private bool _isDark;
    private bool _initialized;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var theme = await JS.InvokeAsync<string>("getTheme");
            _isDark = theme == "dark";
            _initialized = true;
            StateHasChanged();
        }
    }

    private async Task ToggleTheme()
    {
        _isDark = !_isDark;
        await JS.InvokeVoidAsync("setTheme", _isDark ? "dark" : "light");
        StateHasChanged();
    }
}
```

- [ ] **Step 2: Build to verify no compile errors**

```bash
dotnet build ParkTracker
```
Expected: `Build succeeded. 0 Error(s)`

- [ ] **Step 3: Commit**

```bash
git add ParkTracker/Components/Layout/ThemeToggle.razor
git commit -m "feat: add ThemeToggle navbar component"
```

---

### Task 4: Add ThemeToggle to the navbar

**Files:**
- Modify: `ParkTracker/Components/Layout/NavMenu.razor`

The right-side `<ul>` currently starts:
```html
<ul class="navbar-nav ms-auto mb-2 mb-md-0">
    <AuthorizeView>
```

- [ ] **Step 1: Add `<ThemeToggle />` as the first item in the right nav**

```html
<ul class="navbar-nav ms-auto mb-2 mb-md-0">
    <li class="nav-item d-flex align-items-center">
        <ThemeToggle />
    </li>
    <AuthorizeView>
```

- [ ] **Step 2: Build**

```bash
dotnet build ParkTracker
```
Expected: `Build succeeded. 0 Error(s)`

- [ ] **Step 3: Run the app and do a manual smoke test**

```bash
dotnet run --project ParkTracker
```

Open `http://localhost:5000` (or the port shown). Verify:
- The navbar shows a 🌙 icon (or ☀️ if your OS is in dark mode)
- Clicking the icon switches the page to dark/light mode visibly
- Refreshing the page keeps the same mode (localStorage persisted)
- Opening a new tab to any page (e.g. `/parks`, `/Account/Login`) starts in the saved mode

- [ ] **Step 4: Commit**

```bash
git add ParkTracker/Components/Layout/NavMenu.razor
git commit -m "feat: add ThemeToggle to navbar"
```

---

### Task 5: Add CSS dark mode overrides for custom brand colors

**Files:**
- Modify: `ParkTracker/wwwroot/app.css`

Bootstrap 5.3 handles its own components automatically. These overrides cover only the custom green brand colors that exist outside Bootstrap's theming system.

- [ ] **Step 1: Append the dark mode overrides to `app.css`**

Add at the end of `ParkTracker/wwwroot/app.css`:

```css
[data-bs-theme="dark"] a,
[data-bs-theme="dark"] .btn-link {
    color: #74c69d;
}

[data-bs-theme="dark"] .btn-primary {
    background-color: #40916c;
    border-color: #2d6a4f;
}

[data-bs-theme="dark"] .btn-primary:hover,
[data-bs-theme="dark"] .btn-primary:focus,
[data-bs-theme="dark"] .btn-primary:active {
    background-color: #2d6a4f;
    border-color: #2d6a4f;
}

[data-bs-theme="dark"] .btn:focus,
[data-bs-theme="dark"] .btn:active:focus,
[data-bs-theme="dark"] .btn-link.nav-link:focus,
[data-bs-theme="dark"] .form-control:focus,
[data-bs-theme="dark"] .form-check-input:focus {
    box-shadow: 0 0 0 0.1rem white, 0 0 0 0.25rem rgba(116, 198, 157, 0.4);
}

.theme-toggle-btn {
    font-size: 1.1rem;
    padding: 0.5rem 0.85rem;
    line-height: 1;
}
```

- [ ] **Step 2: Run the app and verify dark mode styling**

```bash
dotnet run --project ParkTracker
```

Switch to dark mode and check:
- Links are light green (`#74c69d`), not invisible dark green on a dark background
- "Add Visit" / primary buttons are visible with a lighter green background
- Focus rings use the lighter green alpha
- Map page, parks list, admin page, and login page all look correct in dark mode
- Toggle back to light mode — all colors return to their original values

- [ ] **Step 3: Check the navbar in dark mode**

The navbar uses `background-color: #1b4332` (dark green) in `NavMenu.razor.css` — this looks correct in both modes without any change needed. Verify the 🌙/☀️ button is visible and legible on the dark green navbar background in both light and dark modes.

- [ ] **Step 4: Commit**

```bash
git add ParkTracker/wwwroot/app.css
git commit -m "feat: add dark mode CSS overrides for custom brand colors"
```

---

## Done

All five tasks complete. The feature is fully implemented:
- Theme initializes before first paint (no flash)
- Toggle is in the navbar, visible on all pages
- Preference persists in `localStorage`
- Falls back to OS `prefers-color-scheme` on first visit
- Bootstrap components theme automatically; custom greens have explicit dark overrides
